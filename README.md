# 🎨 Distinguishable Colormap Generator

基于高斯混合模型(GMM)的高可区分性色图生成系统，采用传统 HTML + JavaScript + Flask 架构。

## 🚀 快速开始

### 一键启动（推荐）

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
./start.sh
```

这会自动安装依赖、启动 Flask 服务器，并在浏览器中打开应用。

### 手动启动

1. **安装 Python 依赖**
```bash
pip install -r requirements.txt
```

2. **启动 Flask 服务器**
```powershell
# Windows PowerShell
$env:FLASK_APP="server.py"
flask run -p 5000
```

或者
```bash
# Linux/Mac
export FLASK_APP=server.py
flask run -p 5000
```

3. **打开浏览器访问**
```
http://localhost:5000
```

## 📁 项目结构

```
distinguishable-colormap/
├── templates/            # HTML 模板
│   └── index.html       # 主页面
├── static/               # 静态资源
│   ├── css/             # 样式文件
│   │   └── style.css    # 主样式（现代化设计）
│   ├── js/              # JavaScript 模块
│   │   ├── index.js     # 主交互逻辑
│   │   ├── dataObj.js   # 数据对象类
│   │   ├── render.js    # 渲染功能
│   │   ├── optimizer.js # 模拟退火优化
│   │   ├── example.js   # 示例数据
│   │   └── notification.js # 通知系统
│   ├── data/            # 示例数据集
│   └── img/             # 图像资源
├── server.py            # Flask 后端服务器
├── requirements.txt     # Python 依赖
└── start.bat/sh         # 一键启动脚本
```

## 🛠️ 技术栈

### 前端
- **HTML5** - 语义化标记
- **CSS3** - 现代化样式（CSS 变量、Grid 布局、动画）
- **JavaScript (ES6+)** - 原生 JavaScript
- **D3.js v4** - 数据可视化和颜色处理

### 后端
- **Flask** - Python Web 框架
- **scikit-learn** - 机器学习(GMM)
- **NumPy** - 数值计算
- **flask-cors** - 跨域支持

## ✨ 功能特性

### 核心功能
- 📁 **数据加载**: 上传自定义CSV/TSV数据或使用预置示例
- 🎨 **颜色优化**: 基于模拟退火算法的色图优化
- 🤖 **智能控制点**: GMM自动检测最优控制点数量
- 🌈 **HCL色彩空间**: 保证感知一致性
- 📊 **实时可视化**: 即时预览色图效果
- 🎯 **交互式调整**: 自定义初始色相和颜色数量

### 面板功能
- **Parameter Panel**: 数据加载、颜色数量选择、优化控制
- **Render Panel**: 标量场可视化渲染
- **Colormap Panel**: 色图渐变显示
- **Control Panel**: 控制点颜色轮显示
- **History Panel**: 历史记录管理

### 导出格式
- 💾 CSV格式
- 📋 JSON格式  
- 🔬 ParaView格式
- 📈 VisIt格式


## 🔧 其他命令

**公网访问：**
```bash
flask run -p 5000 -h 0.0.0.0
```

**后台运行：**
```bash
nohup flask run -p 5000 -h 0.0.0.0 &
```

**查看端口进程：**
```bash
# Linux/Mac
lsof -i:5000

# Windows
netstat -ano | findstr :5000
```

**杀死进程：**
```bash
# Linux/Mac
kill -9 <pid>

# Windows
taskkill /PID <pid> /F
```

